@using BaseSample1.Models
@model User[]

@{
    ViewBag.Title = "View Phonebook StrongType";
}

<h2>View Phonebook Strongly typed</h2>

@for (int i = 0; i < Model.Length; i++)
{
    @Model[i].Username @:- @Model[i].Phonenumber
    <text><br /></text>
}

@* with one more bracket
    @{
        for (int i = 0; i < Model.Length; i++)
        {
        @Model[i].Username @:-  @Model[i].Phonenumber
        <text><br /></text>
        }
    }
*@